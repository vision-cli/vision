{
  "pluginName": "infrastructure",
  "use": "infra",
  "flagset": [
    {
      "flagname": "version",
      "shorthand": "v",
      "longhand": "version",
      "descriptor": "get infra plugin version",
      "example": "vision infra create"
    }
  ],
  "commands": [
    {
      "create": {
        "use": "create",
        "short": "create infrastructure",
        "long": "ditto",
        "flagset": [
          {
            "flagname": "cloud-provider",
            "shorthand": "c",
            "longhand": "cloud-provier",
            "descriptor": "select cloud provider",
            "example": "vision infra create -c azure"
          }
        ]
      }
    },
    {
      "create": {
        "use": "destroy",
        "short": "create infrastrcuture",
        "long": "ditto",
        "flagset": [
          {
            "flagname": "version",
            "shorthand": "v",
            "longhand": "version",
            "descriptor": "get infra plugin version",
            "example": "vision infra create"
          }
        ]
      }
    }
  ]
}
